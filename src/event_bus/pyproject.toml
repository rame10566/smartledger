[project]
name = "smartledger-event-bus"
version = "0.1.0"
description = "Redis Streams event bus consumer — delivers events to the AI Agent"
requires-python = ">=3.12"
dependencies = [
    "redis>=5.0",
    "smartledger-shared",
]

[tool.uv.sources]
smartledger-shared = { workspace = true }
